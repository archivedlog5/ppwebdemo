// TODO: implement dropin-ui demo
const { Router } = require('express')
const router = Router()
router.get('/dropin-ui', (req, res) => res.status(200).send('Coming soon — dropin-ui'))
module.exports = router
