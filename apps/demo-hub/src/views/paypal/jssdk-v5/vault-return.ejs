<%- include('../../partials/header', {
  title, provider, sdkVersion, currentProductKey, currentSdkVersion,
  sidebarProducts, showSidebar
}) %>

<div class="sandbox-page">
  <div class="sandbox-header">
    <span class="provider-badge badge-paypal">PayPal · JSSDK v5 · Vault Return</span>
    <h1><%= title %></h1>
    <p>Enter a Payment Token ID to charge amount server-side — no buyer interaction.</p>
  </div>
  <div class="sandbox-card">
    <div class="amount-row">
      <div class="currency-group">
        <label class="field-label" for="demo-currency">Currency</label>
        <select id="demo-currency" class="currency-select" aria-label="Select currency">
          <% var _cur = currency || 'USD' %>
          <% ['USD','EUR','GBP','CAD','AUD','BRL','CHF','CZK','DKK','HKD','HUF','ILS','JPY','MXN','NOK','NZD','PHP','PLN','SEK','SGD','THB','TWD','KRW','CLP','SAR','UYU','COP','IDR','PEN','AED'].forEach(function(c) { %>
            <option value="<%= c %>" <%= _cur === c ? 'selected' : '' %>><%= c %></option>
          <% }) %>
        </select>
      </div>
      <div class="amount-group">
        <label class="field-label" for="demo-amount">Amount</label>
        <div class="amount-input-wrapper">
          <input id="demo-amount" class="amount-input" type="text" inputmode="decimal"
            value="<%= defaultAmount || '100.00' %>" placeholder="0.00"
            aria-label="Payment amount" />
        </div>
      </div>
    </div>
    <div class="amount-error" id="amount-error" role="alert"></div>
    <span class="sandbox-mode-badge" style="display:inline-block;margin-bottom:16px">⚡ Sandbox Mode</span>
    </div>
    <div class="field-group">
      <label class="field-label" for="payment-token-input">Payment Token ID</label>
      <input id="payment-token-input" class="token-input" type="text" placeholder="e.g. PAYMENT-TOKEN-xxxx" aria-label="Payment Token ID" />
    </div>
    <button id="vault-return-btn" class="pay-btn" style="background:var(--accent);color:#0F172A;margin-top:8px" type="button">
      Charge with Vaulted Method
    </button>
    <div class="result-msg" id="result" role="alert" aria-live="polite"></div>
    <div class="test-hint">Get a Payment Token ID from the Vault Setup demos above.</div>
  </div>
</div>

<script>
  window.DEMO = {
    urls: {
      createAndCapture: '/paypal/jssdk-v5/api/vault-return/create-and-capture',
    },
    defaultAmount: '<%= defaultAmount || "100.00" %>',
  }
</script>
<script src="/js/paypal/jssdk-v5/vault-return.js"></script>

<%- include('../../partials/footer', { showSidebar }) %>
